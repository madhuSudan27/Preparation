#include<iostream>
#include<unordered_set>
using namespace std;
int main()
{
    unordered_set<int>st;
    // same like set but unordered 
    // here lower_bound() ans upper_bound() function does not works 
    // rest all function are same 
    // O(1) better time complexity for all operation

     // once in a blue moon 
    // in case of collision may O(n);

    return 0;
}