#include<iostream>
#include<vector>
using namespace std;

/* 
the distance from v to w can be computed as the distance from the root to v, 
plus the distance from the root to w, minus twice the distance from the root to their lowest common ancestor
 */
int main()
{
    return 0;
}